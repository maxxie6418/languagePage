# 英语学习站 — 产品需求文档（PRD）

> **版本**：v1.1  
> **日期**：2026-05-07  
> **技术栈**：Node.js + Express + SQLite3 + 原生 HTML/CSS/JS（单页应用）  
> **AI 服务**：DeepSeek API（后端代理）  
> **目标用户**：英语学习者（单机/小范围多用户，≤50人）

---

## 一、产品定位

一款面向考研英语的**本地化学习管理平台**，覆盖基础学习、真题解析、模拟测验三大主线，集成艾宾浩斯复习辅助、AI 答疑、多用户进度隔离。核心目标：**让用户清晰掌握自身学习进度，并通过结构化内容系统备考**。

---

## 二、信息架构

```
考研英语学习站
├── 首页 / 进度仪表盘（默认落地页）
│   ├── 总体掌握率 + 六维雷达图
│   ├── 艾宾浩斯今日待复习（可开关提醒）
│   └── 智能推荐学习任务
├── 基础学习
│   ├── 单词掌握清单（核心）
│   │   └── 单词检测模式（隐藏中文/英文）
│   ├── 词根词缀库
│   ├── 语法体系
│   │   └── 语法拆解工具（辅助工具）
│   ├── 翻译训练
│   ├── 作文素材
│   ├── 阅读方法论
│   └── 辅助工具区
│       ├── 语法拆解器
│       ├── 单词清单生成器（按状态导出 MD）
│       └── 艾宾浩斯查询器（全局复习日历）
├── 真题解析
│   └── 题型专项（例题形式）
│       ├── 完形填空
│       ├── 阅读理解
│       ├── 翻译
│       └── 作文
├── 模拟测验
│   ├── 快速测验（分题型，~10题）
│   ├── 全真模拟（整套流程）
│   └── 错题回顾
├── 全局设置
│   ├── AI 配置（DeepSeek API Key / BaseURL）
│   ├── 艾宾浩斯提醒开关
│   ├── 数据导入 / 导出（MD / JSON）
│   └── 账号管理（管理员可见）
└── 登录 / 注册
```

---

## 三、用户系统（多用户支持）

### 3.1 用户角色

| 角色 | 权限 |
|------|------|
| 普通用户 | 学习、测验、标记掌握、使用 AI、查看个人进度 |
| 管理员 | 上述全部 + 查看用户列表、重置密码、数据备份/恢复 |

### 3.2 注册与登录

- **注册**：用户名 + 密码（密码 bcrypt 加密存储），用户名唯一，≤50 个账号
- **登录**：JWT Token（有效期 7 天，支持"记住我"），存储于 localStorage
- **未登录状态**：所有页面重定向至登录页
- **首个注册用户**：自动设为管理员角色

### 3.3 数据隔离原则

- 所有用户学习数据（掌握状态、艾宾浩斯记录、错题、测验历史）**按 user_id 隔离**
- 公共内容（单词库、词根词缀、语法章节、真题例题）为**只读共享数据**
- 用户可导出个人数据为 JSON 备份

---

## 四、数据库设计（SQLite3）

### 4.1 用户表 `users`

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | 自增主键 |
| username | TEXT UNIQUE | 用户名 |
| password_hash | TEXT | bcrypt 哈希 |
| role | TEXT | `user` / `admin`，默认 `user` |
| ebbinghaus_reminder_enabled | INTEGER | 艾宾浩斯提醒开关，0/1，默认 1（开启） |
| created_at | DATETIME | 注册时间 |

### 4.2 单词表 `words`（公共只读）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | 自增 |
| word | TEXT | 英文单词 |
| phonetic | TEXT | 音标 |
| meaning | TEXT | 中文释义 |
| example | TEXT | 例句（可选） |
| category | TEXT | 分类标签，如 `高频/中频/低频` |

### 4.3 用户单词掌握表 `user_words`（用户隔离）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | 自增主键 |
| user_id | INTEGER FK | 关联 users.id |
| word_id | INTEGER FK | 关联 words.id |
| status | TEXT | `unmastered`(未掌握) / `learning`(记忆中) / `mastered`(已掌握) |
| mastered_at | DATETIME | 标记为已掌握的时间（艾宾浩斯起点） |
| updated_at | DATETIME | 最后更新时间 |

**约束**：UNIQUE(user_id, word_id)

### 4.4 知识点表 `knowledge_points`（公共只读）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | |
| module | TEXT | 所属模块：`词根词缀/语法/翻译/作文/阅读/真题完形/真题阅读/真题翻译/真题作文` |
| title | TEXT | 标题 |
| content | TEXT | 内容（支持 Markdown） |
| order_index | INTEGER | 排序 |

### 4.5 用户知识点掌握表 `user_knowledge`（用户隔离）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | 自增主键 |
| user_id | INTEGER FK | |
| knowledge_id | INTEGER FK | |
| status | TEXT | `unmastered` / `mastered` |
| mastered_at | DATETIME | 标记已掌握时间 |

**约束**：UNIQUE(user_id, knowledge_id)

### 4.6 艾宾浩斯复习记录表 `ebbinghaus_records`（用户隔离）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | 自增主键 |
| user_id | INTEGER FK | |
| item_type | TEXT | `word` / `knowledge` |
| item_id | INTEGER | 对应 word_id 或 knowledge_id |
| stage | INTEGER | 复习阶段 1~6（对应 1/2/4/7/15/30 天） |
| scheduled_at | DATETIME | 计划复习日期 |
| reviewed_at | DATETIME | 实际复习日期（NULL 表示未复习） |
| status | TEXT | `pending`(待复习) / `done`(已复习) / `skipped`(跳过) |

**约束**：UNIQUE(user_id, item_type, item_id, stage)

### 4.7 测验记录表 `quiz_records`（用户隔离）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | |
| user_id | INTEGER FK | |
| quiz_type | TEXT | `quick`(快速) / `full`(全真) |
| category | TEXT | 题型分类 |
| score | INTEGER | 得分 |
| total | INTEGER | 总分 |
| answers | TEXT (JSON) | 答题详情 |
| created_at | DATETIME | |

### 4.8 错题表 `mistakes`（用户隔离）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | 自增主键 |
| user_id | INTEGER FK | |
| question_id | INTEGER | 关联 questions.id |
| category | TEXT | 题型 |
| wrong_count | INTEGER | 错误次数，默认 1 |
| last_wrong_at | DATETIME | |

**约束**：UNIQUE(user_id, question_id)

### 4.9 AI 配置表 `ai_configs`（全局单条，管理员维护）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | 固定为 1 |
| provider | TEXT | `deepseek` |
| api_key | TEXT | 密钥（后端加密存储） |
| base_url | TEXT | API 基础地址 |
| model | TEXT | 默认模型，如 `deepseek-chat` |
| is_enabled | INTEGER | 0/1，是否启用 AI |

### 4.10 题库表 `questions`（公共只读）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | 自增主键 |
| category | TEXT | 题型分类：`完形填空/阅读理解/翻译/作文` |
| source | TEXT | 来源年份/出处（可选） |
| passage | TEXT | 文章正文（阅读理解/完形填空的文章内容，支持 Markdown） |
| question_text | TEXT | 题目内容（不含选项） |
| options | TEXT (JSON) | 选项列表 JSON，如 `["A. xxx","B. xxx","C. xxx","D. xxx"]`（翻译/作文题型可为 NULL） |
| answer | TEXT | 标准答案（选择题为选项字母，翻译/作文为参考范文或要点） |
| explanation | TEXT | 解析内容（支持 Markdown） |
| keywords | TEXT | 重点词汇（JSON 数组，可选） |
| difficulty | TEXT | 难度：`easy/medium/hard`，默认 `medium` |
| order_index | INTEGER | 排序 |

### 4.11 题集表 `question_sets`（公共只读）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | 自增主键 |
| name | TEXT | 题集名称，如 `2024 阅读真题` |
| type | TEXT | `quick`(快速测验) / `full`(全真模拟) |
| category | TEXT | 题型分类（全真模拟时为 NULL） |
| time_limit | INTEGER | 答题时限（分钟），全真模拟专用 |
| description | TEXT | 题集描述 |

### 4.12 题集题目关联表 `question_set_items`（公共只读）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | 自增主键 |
| set_id | INTEGER FK | 关联 question_sets.id |
| question_id | INTEGER FK | 关联 questions.id |
| order_index | INTEGER | 题号排序 |

**约束**：UNIQUE(set_id, question_id)

### 4.13 用户单词清单表 `user_word_lists`（用户隔离）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | 自增主键 |
| user_id | INTEGER FK | |
| name | TEXT | 清单名称，如 `高频易错词` |
| description | TEXT | 清单描述（可选） |
| created_at | DATETIME | 创建时间 |

### 4.14 用户单词清单条目表 `user_word_list_items`（用户隔离）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | 自增主键 |
| list_id | INTEGER FK | 关联 user_word_lists.id |
| word_id | INTEGER FK | 关联 words.id |
| added_at | DATETIME | 添加时间 |
| note | TEXT | 备注（可选） |

**约束**：UNIQUE(list_id, word_id)

### 4.15 用户单词标记表 `user_word_flags`（用户隔离）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | 自增主键 |
| user_id | INTEGER FK | |
| word_id | INTEGER FK | 关联 words.id |
| flag_type | TEXT | `important`(重点) / `difficult`(难点) / `favorite`(收藏) |
| created_at | DATETIME | 标记时间 |

**约束**：UNIQUE(user_id, word_id, flag_type)

---

## 五、API 接口规范

### 5.1 认证相关

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/auth/register` | 注册（限 ≤50 人） |
| POST | `/api/auth/login` | 登录，返回 JWT |
| POST | `/api/auth/logout` | 登出（客户端清除 Token） |
| GET | `/api/auth/me` | 获取当前用户信息 |

### 5.2 单词相关

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/words` | 获取单词列表（支持分页、搜索、按 category 筛选） |
| GET | `/api/words/:id` | 单个单词详情 |
| POST | `/api/user/words/status` | 更新用户单词掌握状态（含 mastered_at 时间戳） |
| GET | `/api/user/words` | 获取当前用户所有单词掌握状态 |
| GET | `/api/user/words/lists` | 获取用户单词清单列表 |
| POST | `/api/user/words/lists` | 创建单词清单 |
| POST | `/api/user/words/lists/:id/items` | 向清单添加单词 |
| DELETE | `/api/user/words/lists/:id/items/:wordId` | 从清单移除单词 |
| POST | `/api/user/words/flags` | 标记/取消标记单词（important/difficult/favorite） |

### 5.3 知识点相关

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/knowledge` | 按 module 筛选获取知识点列表 |
| GET | `/api/knowledge/:id` | 知识点详情 |
| POST | `/api/user/knowledge/status` | 更新知识点掌握状态 |
| GET | `/api/user/knowledge` | 获取当前用户知识点掌握状态 |

### 5.4 艾宾浩斯相关

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/user/ebbinghaus/today` | 获取今日待复习列表（含单词和知识点） |
| GET | `/api/user/ebbinghaus/calendar` | 获取复习日历（未来 N 天计划） |
| POST | `/api/user/ebbinghaus/review` | 标记某项已复习（更新 status + 推进 stage） |
| GET | `/api/user/ebbinghaus/settings` | 获取提醒开关状态 |
| POST | `/api/user/ebbinghaus/settings` | 更新提醒开关 |

**艾宾浩斯复习逻辑**（预生成策略）：
- 当用户将单词/知识点标记为 `mastered` 时，后端自动在 `ebbinghaus_records` 一次性插入 6 条记录，stage 1~6，scheduled_at 分别 = mastered_at + 1/2/4/7/15/30 天，status = `pending`
- 用户调用 `/review` 时：
  - 若点击"已掌握"：将当前 stage 标记为 `done`（此时下一条 stage 的记录已存在，无需新建）
  - 若点击"未掌握"：将对应 word/knowledge 状态改回 `unmastered`，将该 item 所有 `pending` 状态的复习记录标记为 `skipped`，后续重新标记 mastered 时重新生成全部记录

### 5.5 测验相关

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/quiz/questions` | 获取测验题目（按 category + limit 从 questions 表随机抽取） |
| GET | `/api/quiz/sets` | 获取题集列表 |
| GET | `/api/quiz/sets/:id/questions` | 获取指定题集的全部题目 |
| POST | `/api/quiz/submit` | 提交答案，返回得分与解析 |
| GET | `/api/user/quiz/history` | 测验历史 |
| GET | `/api/user/mistakes` | 错题本 |
| POST | `/api/user/mistakes/clear` | 清空/移除某道错题 |

### 5.6 AI 相关

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/ai/chat` | 通用对话（透传 DeepSeek） |
| POST | `/api/ai/grammar` | 语法拆解（预置 Prompt：拆解句子结构） |
| POST | `/api/ai/word` | 单词深度解析（预置 Prompt：词根、词缀、例句、同义词） |
| POST | `/api/ai/essay` | 作文批改（预置 Prompt：考研英语作文评分标准） |

**AI 安全**：API Key 仅存储于后端，前端不接触。后端调用时附加用户上下文（如当前单词、当前语法章节）。

### 5.7 数据导入/导出

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/admin/import/words` | **管理员**上传 MD 文件导入单词 |
| POST | `/api/admin/import/knowledge` | **管理员**上传 MD 文件导入知识点 |
| POST | `/api/admin/import/questions` | **管理员**上传 JSON/MD 文件导入题库 |
| GET | `/api/user/export` | 导出当前用户全部数据为 JSON |
| POST | `/api/user/import` | 导入用户个人数据（JSON，覆盖策略：已存在的记录跳过） |
| GET | `/api/admin/export/all` | **管理员**导出全站数据备份 |
| POST | `/api/admin/import/restore` | **管理员**恢复全站数据（上传备份 JSON，覆盖前校验格式） |

### 5.8 设置与管理

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/admin/users` | 用户列表（管理员） |
| POST | `/api/admin/users/:id/reset-password` | 重置密码（管理员） |
| DELETE | `/api/admin/users/:id` | 删除用户（管理员，不可删除自身及最后一个管理员） |
| GET | `/api/admin/ai-config` | 获取 AI 配置 |
| POST | `/api/admin/ai-config` | 更新 AI 配置 |

**删除用户约束**：
- 不允许管理员删除自身
- 若目标用户是管理员，且系统中仅剩该一个管理员，则拒绝删除
- 删除用户时，其所有学习数据（user_words、user_knowledge、ebbinghaus_records、quiz_records、mistakes、user_word_lists、user_word_flags）**级联删除**
- 被删除的用户名不可复用（保留 users 记录但标记为 `deleted`，或彻底删除并释放用户名）

---

## 六、页面布局与交互设计

### 6.1 全局布局

```
┌─────────────────────────────────────────────┐
│  [Logo]  首页  基础学习  真题解析  模拟测验  设置  [用户名▼]  │
├─────────────────────────────────────────────┤
│                                             │
│              主内容区（下方详述）              │
│                                             │
└─────────────────────────────────────────────┘
```

- **顶部导航栏**：固定，高度 56px，当前页面高亮
- **用户下拉菜单**：个人进度、数据导出、登出
- **管理员额外入口**：账号管理、数据导入

### 6.2 登录/注册页

- 居中卡片布局，宽 400px
- 登录：用户名 + 密码 + 记住我 + 登录按钮
- 注册：用户名 + 密码 + 确认密码 + 注册按钮（满 50 人时隐藏注册入口，提示联系管理员）

### 6.3 首页 / 进度仪表盘

```
┌─────────────────────────────────────────────────────────────┐
│  [总体掌握率 68%]  [考研倒计时 230天]                        │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│              [六维雷达图：单词/词根/语法/翻译/作文/阅读]        │
│                                                             │
├─────────────────────────────────────────────────────────────┤
│  📋 艾宾浩斯今日待复习（提醒：开/关）                        │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐                       │
│  │ abandon │ │ 定语从句 │ │ ...     │                       │
│  │ [复习]  │ │ [复习]  │ │         │                       │
│  └─────────┘ └─────────┘ └─────────┘                       │
├─────────────────────────────────────────────────────────────┤
│  🎯 推荐学习任务                                            │
│  1. 单词掌握率最低，建议复习「高频词」中 30 个未掌握单词      │
│  2. 语法「非谓语动词」已到期复习                              │
│  3. 错题本中「阅读理解」错误率最高，建议专项测验              │
└─────────────────────────────────────────────────────────────┘
```

- **雷达图**：六维数据来源于用户在各模块的掌握率
  - 单词：已掌握单词数 / 单词总数
  - 词根词缀：已掌握条目 / 总条目
  - 语法：已掌握章节 / 总章节
  - 翻译：已掌握篇目 / 总篇目
  - 作文：已掌握篇目 / 总篇目
  - 阅读：已掌握篇目 / 总篇目
- **艾宾浩斯区块**：可折叠，开关控制是否显示。到期卡片显示「复习」按钮，点击后弹出确认（已掌握/未掌握）
- **推荐算法**：
  1. 优先推荐掌握率最低的维度
  2. 其次推荐艾宾浩斯到期内容
  3. 最后推荐错题本高频错误题型

### 6.4 基础学习页

```
┌────────┬──────────────────────────────────────────────────────┐
│        │  [🔍 搜索] [筛选▼全部] [视图切换 列表/卡片]          │
│  单词  │  ┌──────────────────────────────────────────────┐   │
│  词根  │  │  abandon  /əˈbændən/              [●]已掌握   │   │
│  语法  │  │  v. 放弃，抛弃                                  │   │
│  翻译  │  │  [AI解析] [例句]                                │   │
│  作文  │  └──────────────────────────────────────────────┘   │
│  阅读  │  ┌──────────────────────────────────────────────┐   │
│  工具  │  │  ability  /əˈbɪləti/              [○]记忆中   │   │
│        │  │  n. 能力                                        │   │
│        │  │  [AI解析] [例句]                                │   │
│        │  └──────────────────────────────────────────────┘   │
│        │                                                      │
└────────┴──────────────────────────────────────────────────────┘
```

**左侧边栏**：
- 单词掌握清单
- 词根词缀库
- 语法体系
- 翻译训练
- 作文素材
- 阅读方法论
- 辅助工具区（语法拆解器、单词清单生成器、艾宾浩斯查询器）

#### 6.4.1 单词掌握清单（核心交互）

**顶部工具栏**（从左到右）：
1. 搜索框：实时搜索单词/释义
2. 状态筛选器：全部 / 未掌握 / 记忆中 / 已掌握
3. 视图切换：列表视图 / 卡片视图
4. **单词检测按钮**：点击后进入检测模式
5. 批量选择开关（开启后所有条目显示复选框）

**单词检测模式**：
- 按钮变为「退出检测」
- 出现两个切换开关：「隐藏中文」「隐藏英文」（可同时关闭，不可同时开启）
- 隐藏中文时：释义区域显示为 `*** 点击显示 ***`，点击后临时展示
- 隐藏英文时：单词区域显示为 `*** 点击显示 ***`
- 状态切换在检测模式下依然可用

---

**列表视图（高密度）**

```
┌────┬──────────┬─────────────────┬────────────┬────────────┬─────────────────┐
│ □  │ 单词      │ 音标            │ 释义        │ 状态       │ 操作            │
├────┼──────────┼─────────────────┼────────────┼────────────┼─────────────────┤
│ □  │ abandon  │ /əˈbændən/      │ v.放弃,抛弃 │ ●已掌握    │ [展开▼] [AI]    │
│ □  │ ability  │ /əˈbɪləti/      │ n.能力,才能 │ ○记忆中    │ [展开▼] [AI]    │
│ □  │ absolute │ /ˈæbsəluːt/     │ adj.绝对的  │ ○未掌握    │ [展开▼] [AI]    │
│ □  │ absorb   │ /əbˈzɔːrb/      │ v.吸收,吸引 │ ○未掌握    │ [展开▼] [AI]    │
│ □  │ abstract │ /ˈæbstrækt/     │ adj.抽象的  │ ○未掌握    │ [展开▼] [AI]    │
└────┴──────────┴─────────────────┴────────────┴────────────┴─────────────────┘
```

- **行高**：紧凑 44px，无多余留白
- **列宽分配**：复选框 40px | 单词 15% | 音标 18% | 释义 35% | 状态 12% | 操作 剩余
- **状态列**：彩色圆点 + 文字标签（绿=已掌握 / 黄=记忆中 / 灰=未掌握），点击直接循环切换
- **操作列**：「展开▼」按钮 + 「AI」小图标按钮
- **点击单词文本**：直接展开该行详情（与「展开▼」按钮同效）
- **行悬停**：浅灰背景高亮，操作按钮由灰变深可见
- **表头固定**：滚动时表头吸顶
- **每屏显示**：桌面端约 15~18 行

**列表视图 — 展开详情行**

点击某行后，该行下方插入展开区域（不跳转、不弹窗）：

```
┌─────────────────────────────────────────────────────────────────────────────┐
│  □  │ abandon  │ /əˈbændən/  │ v.放弃,抛弃  │ ●已掌握  │ [收起▲] [AI]    │
├─────────────────────────────────────────────────────────────────────────────┤
│  📖 例句                                                                    │
│     She decided to abandon her career and travel the world.                 │
│     她决定放弃事业，去周游世界。                                             │
│                                                                             │
│  🔗 词根：a- (加强) + bandon (权力,管辖) → 放弃管辖权 → 放弃                 │
│  🏷️ 搭配：abandon oneself to despair 陷入绝望                               │
│                                                                             │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐                         │
│  │ ○ 未掌握    │  │ ○ 记忆中    │  │ ● 已掌握    │  ← 状态快速切换          │
│  └─────────────┘  └─────────────┘  └─────────────┘                         │
│                                                                             │
│  [🤖 AI深度解析]  [📋 加入单词清单]  [📌 标记重点]                           │
└─────────────────────────────────────────────────────────────────────────────┘
```

- 展开区域背景略深于列表底色，左侧加 3px 彩色竖条（颜色对应当前状态）
- 同时只能展开一行，展开新行时自动收起旧行
- 展开动画：高度从 0 过渡到 auto，时长 200ms

---

**卡片视图（中高密度）**

```
┌──────────────────────────┐ ┌──────────────────────────┐ ┌──────────────────────────┐
│  □  abandon               │ │  □  ability               │ │  □  absolute              │
│  /əˈbændən/               │ │  /əˈbɪləti/               │ │  /ˈæbsəluːt/              │
│  v. 放弃，抛弃            │ │  n. 能力，才能            │ │  adj. 绝对的，完全的      │
│                           │ │                           │ │                           │
│  [● 已掌握]  [展开▼] [AI] │ │  [○ 记忆中]  [展开▼] [AI] │ │  [○ 未掌握]  [展开▼] [AI] │
└──────────────────────────┘ └──────────────────────────┘ └──────────────────────────┘
┌──────────────────────────┐ ┌──────────────────────────┐ ┌──────────────────────────┐
│  □  absorb                │ │  □  abstract              │ │  □  abundant              │
│  /əbˈzɔːrb/               │ │  /ˈæbstrækt/              │ │  /əˈbʌndənt/              │
│  v. 吸收，吸引            │ │  adj. 抽象的  n. 摘要     │ │  adj. 丰富的，大量的      │
│                           │ │                           │ │                           │
│  [○ 未掌握]  [展开▼] [AI] │ │  [○ 未掌握]  [展开▼] [AI] │ │  [○ 未掌握]  [展开▼] [AI] │
└──────────────────────────┘ └──────────────────────────┘ └──────────────────────────┘
```

- **卡片尺寸**：固定宽度（如 280px），高度自适应，间距 12px
- **布局**：CSS Grid，自动换行，桌面端每行 3~4 张
- **卡片内容**：
  - 顶部：复选框 + 单词（大号加粗，18px）
  - 音标（灰色，14px）
  - 释义（主内容，14px，最多显示 2 行，超出省略）
  - 底部：状态按钮（胶囊样式）+ 展开按钮 + AI 按钮
- **卡片悬停**：轻微上浮阴影（`translateY(-2px)` + `box-shadow`）
- **点击单词文本**：展开该卡片详情

**卡片视图 — 展开详情**

点击后卡片高度扩展，内部展开详情区域：

```
┌──────────────────────────┐
│  □  abandon               │
│  /əˈbændən/               │
│  v. 放弃，抛弃            │
│  ───────────────────────  │
│  📖 例句：                 │
│  She decided to abandon   │
│  her career...            │
│                           │
│  🔗 词根：a- + bandon      │
│  🏷️ 搭配：abandon...       │
│  ───────────────────────  │
│  [○未掌握][○记忆中][●已掌握]│
│  [🤖AI解析] [📋加入清单]    │
│  [▲ 收起]                 │
└──────────────────────────┘
```

- 展开时卡片宽度不变，高度自适应增加
- 展开动画：高度过渡 200ms
- 同时可展开多张卡片（与列表视图不同，不强制互斥）
- 展开后卡片边框加粗或变色，视觉区分

---

**两种视图通用交互**

| 交互 | 列表视图 | 卡片视图 |
|------|---------|---------|
| 展开详情 | 行下方插入展开区，互斥 | 卡片内部扩展，可多开 |
| 状态切换 | 点击状态列直接循环 | 点击底部状态胶囊循环 |
| 单词检测 | 隐藏列内容，行高不变 | 隐藏卡片内对应区域 |
| 批量选择 | 复选框 + 底部浮条 | 复选框 + 底部浮条 |
| 每屏容量 | ~15-18 行 | ~6-8 张（3列） |
| 适合场景 | 快速浏览、批量操作 | 深度学习、逐个击破 |

**批量操作浮条**（两种视图通用）：
- 选中 ≥1 项后，页面底部固定浮出工具条
- 内容：「已选 N 项」+「批量标记为记忆中」+「批量标记为已掌握」+「取消选择」
- 位置：距底部 24px，居中，圆角阴影卡片

#### 6.4.2 语法体系

- 左侧显示章节树形列表（如：句子成分 → 主谓宾 / 定语从句 / 状语从句...）
- 右侧显示章节内容（Markdown 渲染）
- 内容底部：「标记为已掌握」按钮
- 右上角：「使用语法拆解工具」按钮，点击后弹出输入框，输入长难句后调用 AI 解析并可视化展示结构

#### 6.4.3 作文素材与批改

**作文素材区**：
- 左侧分类列表（图表作文、议论文、应用文等）
- 右侧范文/模板内容展示
- 可标记已掌握

**作文批改输入界面**：
- 标题输入框（可选）
- 正文输入区（多行文本框，带字数统计实时显示）
- 字数限制提示（考研英语作文要求）
- 「提交批改」按钮
- 历史记录入口（查看过往批改记录）
- 批改结果展示：评分（内容/结构/语言/字数四维评分）、修改建议、原文对比高亮

#### 6.4.4 辅助工具区

**语法拆解器**：
- 输入框 + 「拆解」按钮
- 结果区：树状结构展示句子成分（主语[红色] / 谓语[蓝色] / 宾语[绿色] / 从句[虚线框]）
- 下方附 AI 文字解析

**单词清单生成器**：
- 筛选条件：状态（多选）、分类（高频/中频/低频）
- 生成按钮 → 输出 MD 格式文本框，支持一键复制

**艾宾浩斯查询器**：
- 日历视图（月历），有复习计划的日期标记圆点
- 点击日期显示当天所有待复习条目
- 支持查看「未来 7 天 / 30 天」计划

### 6.5 真题解析页

```
┌─────────────────────────────────────────────────────────────┐
│  [完形填空] [阅读理解] [翻译] [作文]  ← 题型 Tab             │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  [例题 1/5]                                                 │
│                                                             │
│  [文章/题目内容区域]                                         │
│                                                             │
│  A. xxx    B. xxx    C. xxx    D. xxx                      │
│                                                             │
│  [查看解析] [标记已掌握] [下一题]                            │
│                                                             │
│  ── 解析区（默认折叠） ──                                    │
│  正确答案：B                                                │
│  解析：...                                                  │
│  重点词汇：...                                              │
│  长难句拆解：...                                            │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

- 不按年份，按题型专项组织
- 每个题型下 5~10 道代表性例题
- 解析区默认折叠，点击展开
- 每道题可独立标记「已掌握」

### 6.6 模拟测验页

**快速测验**：
- 选择题型（完形/阅读/翻译/作文/混合）+ 题数（5/10/20）
- 开始测验 → 逐题展示（单题单页或滚动）
- 提交后即时判卷，显示得分、正确答案、解析
- 自动将错题录入错题本

**全真模拟**：
- 按考研英语试卷结构生成一套完整模拟题
- 流程：答题 → 提交 → 结果页（总分 + 各题型得分 + 耗时）
- 不记录日常学习时长，仅记录测验耗时

**错题回顾**：
- 列表展示所有错题，按错误次数降序
- 每题显示：题目摘要、错误次数、最后错误时间
- 操作：「重新作答」「查看解析」「移除错题」
- 支持按题型筛选

### 6.7 设置页

```
┌─────────────────────────────────────────────────────────────┐
│  AI 配置                                                    │
│  ─────────────────────────────────────────────────────────  │
│  启用 AI：[开关]                                             │
│  API Key：[••••••••]                                        │
│  BaseURL：[https://api.deepseek.com/v1]                     │
│  模型：[deepseek-chat ▼]                                    │
│                                                             │
│  艾宾浩斯设置                                               │
│  ─────────────────────────────────────────────────────────  │
│  首页提醒：[开关]                                            │
│  复习间隔：1天 → 2天 → 4天 → 7天 → 15天 → 30天（只读展示） │
│                                                             │
│  数据管理                                                   │
│  ─────────────────────────────────────────────────────────  │
│  [导出我的学习数据]  [导入学习数据]                          │
│                                                             │
│  管理员专区（仅管理员可见）                                  │
│  ─────────────────────────────────────────────────────────  │
│  [用户管理] [导入单词MD] [导入知识点MD] [全站备份]           │
└─────────────────────────────────────────────────────────────┘
```

---

## 七、内容数据策略

### 7.1 初始内置数据

| 内容 | 数量 | 来源 |
|------|------|------|
| 考研高频单词 | 100~200 个 | AI 收集整理 |
| 词根词缀 | 30~50 组 | AI 收集整理 |
| 语法章节 | 10~15 章 | AI 收集整理 |
| 翻译/作文/阅读素材 | 各 3~5 篇 | AI 收集整理 |
| 真题例题 | 各题型 3~5 道 | AI 收集整理 |

### 7.2 MD 导入格式规范

**单词导入格式（`words.md`）**：
```markdown
# 单词表

## 高频词
- **abandon** /əˈbændən/ v. 放弃，抛弃
- **ability** /əˈbɪləti/ n. 能力，才能

## 中频词
- **candidate** /ˈkændɪdeɪt/ n. 候选人
```

**知识点导入格式（`knowledge.md`）**：
```markdown
# 语法体系

## 定语从句
定语从句（Attributive Clause）是指在复合句中修饰名词或代词的从句...

## 非谓语动词
非谓语动词包括不定式、动名词和分词...

# 作文素材

## 图表作文模板
As is vividly shown in the chart...
```

**导入规则**：
- 后端按 `#` 一级标题识别模块名（如 `语法体系` → module=`语法`）
- 按 `##` 二级标题识别条目标题
- 正文为 content
- 支持批量上传，重复标题自动跳过

### 7.3 题库导入格式规范（`questions.json`）

```json
[
  {
    "category": "阅读理解",
    "passage": "文章正文内容...",
    "question_text": "According to the passage, what is the main idea?",
    "options": ["A. Option A", "B. Option B", "C. Option C", "D. Option D"],
    "answer": "B",
    "explanation": "解析内容...",
    "keywords": ["keyword1", "keyword2"],
    "difficulty": "medium"
  }
]
```

**字段说明**：
- `category`：题型分类，支持 `完形填空/阅读理解/翻译/作文`
- `passage`：文章正文（选择题型必填，翻译/作文可为空）
- `options`：选项列表（翻译/作文题型可为 null）
- `answer`：标准答案（选择题为选项字母，翻译/作文为参考范文或要点）
- `explanation`：解析（支持 Markdown）
- `keywords`：重点词汇数组（可选）
- `difficulty`：难度 `easy/medium/hard`，默认 `medium`

**MD 导入格式（`questions.md`）**：

```markdown
# 阅读理解

## 题 1
> 文章正文...

**题目**：According to the passage, what is the main idea?
A. Option A
B. Option B
C. Option C
D. Option D

**答案**：B
**解析**：解析内容...
**难度**：medium
```

---

## 八、艾宾浩斯记忆法详细逻辑

### 8.1 触发条件（预生成策略）
用户将单词或知识点标记为 `mastered` 时，后端自动执行：

```
for stage in [1, 2, 3, 4, 5, 6]:
    interval = [1, 2, 4, 7, 15, 30][stage-1]  # 天
    scheduled_at = now + interval days
    插入 ebbinghaus_records:
        user_id, item_type, item_id, stage, scheduled_at, status='pending'
```

一次性生成全部 6 条记录，后续复习流程仅更新 status，不再新增。

### 8.2 复习流程

1. 用户首页看到「今日待复习」卡片
2. 点击「复习」按钮 → 弹出对话框：
   - **已掌握**：将当前 stage 标记为 `done`（后续 stage 记录已存在，无需新建）
   - **未掌握**：将对应 item 状态改回 `unmastered`，将该 item 所有 `pending` 状态的复习记录标记为 `skipped`。后续重新标记 mastered 时将重新生成全部 6 条记录
3. 用户也可在「艾宾浩斯查询器」中提前查看未来计划

### 8.3 提醒开关

- 开关存储于后端 `users` 表扩展字段 `ebbinghaus_reminder_enabled`（布尔值），支持多设备同步
- 关闭时：首页不显示「今日待复习」区块，但数据仍记录，查询器中仍可查看
- 开启时：首页顶部或侧边显示待复习数量角标

---

## 九、AI 功能详细设计

### 9.1 配置

- 管理员在设置页配置 DeepSeek API Key 和 BaseURL
- 后端存储，调用时读取
- 支持开关控制，关闭后所有 AI 按钮隐藏或置灰

### 9.2 功能入口与预置 Prompt

| 入口 | 功能 | 预置 Prompt 要点 |
|------|------|-----------------|
| 单词卡片「AI 解析」 | 深度查词 | "请详细解析单词 {word}，包括：词根词缀拆解、核心释义、考研常见搭配、2个真题风格例句" |
| 语法拆解工具 | 长难句分析 | "请拆解以下句子的语法结构，用树状图形式标注主谓宾、从句类型，并给出中文翻译：{sentence}" |
| 作文页「AI 批改」 | 作文评分 | "请按考研英语作文评分标准（内容、结构、语言、字数）对以下作文打分并给出修改建议：{essay}" |
| 全局悬浮窗 | 自由问答 | 通用对话，上下文保留最近 5 轮 |

### 9.3 交互形式

- AI 结果以**流式输出**（SSE）展示在卡片或弹窗中
- 支持「复制」「重新生成」
- 错误时显示友好提示（如"AI 服务暂不可用"）

---

## 十、非功能性需求

### 10.1 性能

- 页面首屏加载 < 2s
- 单词列表支持虚拟滚动（若后续数据量大）
- API 响应 < 500ms（AI 调用除外）

### 10.2 安全

- 密码 bcrypt 加密（强度 10）
- JWT 鉴权（有效期 7 天），存储于 localStorage
- JWT 过期策略：后端维护 Token 黑名单，密码重置后旧 Token 立即失效
- 登出流程：前端清除 localStorage 中的 Token，后端将该 Token 加入黑名单（有效期内保持）
- CSRF 防护：使用 Cookie + Header 双重验证（后端检查 Origin 头）
- XSS 防护：前端输入内容进行 HTML 转义，后端输出时进行内容过滤
- AI API Key 仅后端持有，前端不可见
- 注册上限 50 人，超限返回友好提示
- 数据备份与恢复：管理员导出数据时进行完整性校验，恢复前确认覆盖策略，支持回滚至备份点

### 10.3 兼容性

- 现代浏览器（Chrome/Firefox/Edge/Safari 最新两版）
- 桌面端为主，平板自适应
- 无需移动端 APP

### 10.4 部署

- 单文件 SQLite 数据库，便于备份迁移
- 提供 `start.bat` / `start.sh` 一键启动
- 默认端口 3000，可配置

---

## 十一、开发优先级（建议迭代顺序）

| 迭代 | 内容 | 说明 |
|------|------|------|
| **MVP** | 用户系统 + 单词清单 + 掌握标记 + 首页仪表盘 | 核心闭环 |
| **V1.1** | 词根词缀 + 语法 + 其他基础学习模块 + 知识点掌握标记 | 完善基础学习 |
| **V1.2** | 艾宾浩斯系统 + 辅助工具区 | 记忆辅助 |
| **V1.3** | 真题解析（题型专项）+ 模拟测验 + 错题本 | 测验闭环 |
| **V1.4** | DeepSeek AI 接入 + 设置页 | 智能辅助 |
| **V1.5** | MD 导入/导出 + 管理员功能 + 数据备份 | 内容管理 |

---

## 十二、附录：Mock 数据示例

### 单词表示例（前 5 条）

| word | phonetic | meaning | category |
|------|----------|---------|----------|
| abandon | /əˈbændən/ | v. 放弃，抛弃 | 高频 |
| ability | /əˈbɪləti/ | n. 能力，才能 | 高频 |
| absolute | /ˈæbsəluːt/ | adj. 绝对的，完全的 | 高频 |
| absorb | /əbˈzɔːrb/ | v. 吸收，吸引 | 高频 |
| abstract | /ˈæbstrækt/ | adj. 抽象的 n. 摘要 | 中频 |

### 语法章节示例

| module | title | content |
|--------|-------|---------|
| 语法 | 定语从句 | 定语从句是指在复合句中修饰名词或代词的从句... |
| 语法 | 非谓语动词 | 非谓语动词包括不定式、动名词和分词三种形式... |
| 语法 | 虚拟语气 | 虚拟语气用来表示说话人的假设、愿望或推测... |

---

*文档结束*

---

## 十三、页面设计清单（供 UI 设计师）

以下列出本项目全部需要独立设计的页面/界面，按设计优先级排列。设计师可据此规划工作量。

### 核心页面（必须设计）

| 序号 | 页面名称 | 说明 | 关键元素 |
|------|---------|------|---------|
| P01 | **登录/注册页** | 用户入口 | 居中登录卡片、用户名/密码输入、记住我、注册切换、错误提示 |
| P02 | **首页 / 进度仪表盘** | 默认落地页，核心数据概览 | 总体掌握率大数字、六维雷达图、艾宾浩斯待复习卡片区（可折叠）、推荐学习任务列表、顶部导航栏 |
| P03 | **基础学习 — 单词掌握清单（列表视图）** | 核心学习场景，高密度信息 | 顶部工具栏（搜索/筛选/视图切换/检测模式/批量选择）、紧凑列表（复选框/单词/音标/释义/状态/操作）、展开详情行（例句/词根/搭配/状态切换/AI按钮）、底部批量操作浮条 |
| P04 | **基础学习 — 单词掌握清单（卡片视图）** | 核心学习场景，中密度信息 | 同 P03 顶部工具栏、Grid 卡片布局（单词/音标/释义/状态胶囊/操作）、卡片内展开详情区、底部批量操作浮条 |
| P05 | **基础学习 — 单词检测模式** | 自测场景，两种视图均需覆盖 | 顶部检测工具栏（隐藏中文/隐藏英文开关）、列表/卡片中隐藏对应内容显示为占位符、检测模式下状态切换依然可用 |
| P06 | **基础学习 — 词根词缀库** | 共享基础学习布局 | 左侧导航高亮"词根词缀"、右侧条目列表（词根/含义/例词）、可标记已掌握 |
| P07 | **基础学习 — 语法体系** | 共享基础学习布局 | 左侧章节树形导航、右侧 Markdown 内容渲染区、底部"标记已掌握"按钮、右上角"语法拆解工具"入口 |
| P08 | **基础学习 — 翻译训练** | 共享基础学习布局 | 左侧篇目列表、右侧原文/译文对照、可标记已掌握 |
| P09 | **基础学习 — 作文素材** | 共享基础学习布局 | 左侧素材分类列表、右侧范文/模板内容、可标记已掌握、AI 批改入口 |
| P10 | **基础学习 — 阅读方法论** | 共享基础学习布局 | 左侧技巧分类列表、右侧内容区、可标记已掌握 |
| P11 | **真题解析 — 题型专项** | 例题学习 | 顶部题型 Tab（完形/阅读/翻译/作文）、例题内容区（文章/题目/选项）、解析折叠区（答案/解析/重点词汇/长难句）、标记已掌握按钮、题号导航 |
| P12 | **模拟测验 — 快速测验** | 分题型测验 | 开始配置（题型选择/题数选择）、答题页（逐题/滚动）、提交后结果页（得分/解析/错题标记） |
| P13 | **模拟测验 — 全真模拟** | 整套试卷模拟 | 开始页、答题页（按考研流程）、结果页（总分/各题型得分/耗时） |
| P14 | **模拟测验 — 错题回顾** | 错题管理 | 错题列表（题目摘要/错误次数/时间）、筛选器（按题型）、操作按钮（重新作答/查看解析/移除） |
| P15 | **设置页** | 全局配置 | AI 配置区（开关/Key/BaseURL/模型）、艾宾浩斯设置（提醒开关/间隔展示）、数据管理（导出/导入）、管理员专区（用户管理/MD导入/全站备份） |

### 弹窗/浮层/嵌入界面（需设计）

| 序号 | 界面名称 | 触发位置 | 关键元素 |
|------|---------|---------|---------|
| M01 | **单词展开详情（列表）** | P03 点击单词/展开按钮 | 例句、词根、搭配、状态切换按钮组、AI解析/加入清单/标记重点 |
| M02 | **单词展开详情（卡片）** | P04 点击单词/展开按钮 | 同 M01，适配卡片内布局 |
| M03 | **语法拆解工具** | P07 右上角按钮 | 输入框、拆解按钮、树状结构结果展示、AI文字解析 |
| M04 | **单词清单生成器** | 辅助工具区 | 筛选条件（状态/分类）、生成按钮、MD输出文本框、复制按钮 |
| M05 | **艾宾浩斯查询器** | 辅助工具区 | 月历视图、日期标记、当日复习条目列表、未来7天/30天切换 |
| M06 | **艾宾浩斯复习确认弹窗** | 首页/查询器点击复习 | 复习条目信息、"已掌握"按钮（推进下一阶段）、"未掌握"按钮（重置状态） |
| M07 | **AI 对话浮窗** | 全局悬浮或各页面 AI 按钮 | 对话历史、输入框、流式输出区域、复制/重新生成按钮 |
| M08 | **AI 作文批改结果** | P09 点击 AI 批改 | 评分展示（内容/结构/语言/字数）、修改建议、原文对比 |
| M09 | **数据导入弹窗** | P15 设置页 | 文件拖拽区、MD格式提示、上传进度、导入结果反馈 |
| M10 | **批量操作浮条** | P03/P04 选中后 | 已选数量、批量标记按钮组、取消选择 |
| M11 | **用户管理面板** | P15 管理员专区 | 用户列表（用户名/角色/注册时间）、重置密码按钮、删除用户 |

### 特殊状态界面（需设计）

| 序号 | 状态 | 出现场景 | 设计要点 |
|------|------|---------|---------|
| S01 | **空状态 — 无单词** | 首次使用或筛选无结果 | 插画/图标 + "暂无单词，去导入或开始学习"引导 |
| S02 | **空状态 — 无错题** | 错题本为空 | 插画 + "还没有错题，继续保持！" |
| S03 | **空状态 — 艾宾浩斯无待复习** | 今日无到期复习 | 插画 + "今日无复习任务，继续学习新知识吧" |
| S04 | **加载中** | 数据请求时 | 骨架屏或旋转加载器，列表/卡片区域占位 |
| S05 | **错误状态** | API 失败 | 错误图标 + 重试按钮 + 友好提示文案 |
| S06 | **注册上限** | 满50人时注册 | 提示文案 + 联系管理员引导 |

---

*页面清单结束*
